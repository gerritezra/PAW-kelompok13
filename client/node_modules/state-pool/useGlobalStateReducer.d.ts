import { GlobalState } from './GlobalState';
declare type Reducer = (currentState: any, newState: any) => any;
declare type Config = {
    default?: any;
    selector?: (state: any) => any;
    patcher?: (state: any, selectedState: any) => any;
    persist: boolean;
};
declare function useGlobalStateReducer(reducer: Reducer, globalState: string | GlobalState, config?: Config): [any, (action: any) => any];
export { useGlobalStateReducer };
