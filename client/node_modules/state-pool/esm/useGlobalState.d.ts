import { GlobalState } from './GlobalState';
declare type Config = {
    default?: any;
    selector?: (state: any) => any;
    patcher?: (state: any, selectedState: any) => any;
    persist: boolean;
};
declare function useGlobalState(globalState: string | GlobalState, config?: Config): [any, (value: any) => any, (state: any) => any];
export { useGlobalState };
